<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="id.sch.smktelkom_mlg.project.xirpl609182736.goals.Goals"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true"/>

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="18"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="id.sch.smktelkom_mlg.project.xirpl609182736.goals.Goals.Application"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="id.sch.smktelkom_mlg.project.xirpl609182736.goals.Goals.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="id.sch.smktelkom_mlg.project.xirpl609182736.goals.Goals.ListActivity"/>
        <activity android:name="id.sch.smktelkom_mlg.project.xirpl609182736.goals.Goals.TaskActivity"/>
        <activity android:name="id.sch.smktelkom_mlg.project.xirpl609182736.goals.Goals.ImageActivity"/>
        <activity android:name="id.sch.smktelkom_mlg.project.xirpl609182736.goals.Goals.ShareActivity"/>

        <!-- Facebook login -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id"/>
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            tools:replace="android:theme"/>
    </application>
</manifest>